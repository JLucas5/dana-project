data Thing{
  int atr1
  char atr2[]
}

component provides intf.Sorter requires data.query.Sort sorter{

  Thing[] sort(Object array[]){
    sorter.sort(array[], Thing.atr1, true)
    return array[]
  }
}
